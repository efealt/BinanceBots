# Phase 3.7 — Strategy Module Structure

Status: PLANNED

## Objective

Restructure strategy code before Phase 4 so each strategy has its own module instead of expanding one shared `grid.rs` file.

This phase is organizational only. It must not change strategy behavior, backtest results, execution semantics, parameters, or persistence.

## Target structure

```text
src/trading/
  mod.rs
  types.rs
  portfolio.rs
  simulation.rs
  strategies/
    mod.rs
    static_grid.rs
    dynamic_grid.rs
    volatility_grid.rs
    mean_reversion.rs
    breakout.rs
```

The current Phase 3 fixture moves from `src/trading/grid.rs` to:

`src/trading/strategies/static_grid.rs`

The other four files are reserved strategy modules only. They must not contain invented trading rules until those strategies are explicitly designed.

## Rules

- One strategy concept gets its own module/file.
- Complex strategies may later become their own folder/module without changing the shared engine.
- Shared strategy contracts stay in `src/trading/types.rs`.
- Portfolio/accounting stays in `src/trading/portfolio.rs`.
- Simulated execution stays in `src/trading/simulation.rs`.
- Backtest, Paper, and Live must all call the same strategy implementation.
- Do not create `*_backtest.rs`, `*_paper.rs`, or `*_live.rs` copies of a strategy.
- Preserve the current public imports where practical so the restructure does not force unrelated code changes.

## Steps

### 3.7.1 — Create strategy namespace

- [ ] Add `src/trading/strategies/mod.rs`.
- [ ] Add separate files for `static_grid`, `dynamic_grid`, `volatility_grid`, `mean_reversion`, and `breakout`.
- [ ] Register those modules from the strategy namespace.

### 3.7.2 — Move the existing strategy

- [ ] Move `GridAnchor`, `StaticGridConfig`, `StaticGridStrategy`, and its unit test from `src/trading/grid.rs` into `src/trading/strategies/static_grid.rs`.
- [ ] Preserve strategy ID, version, parameters, initialization, order generation, and validation exactly.
- [ ] Remove the old `src/trading/grid.rs`.

### 3.7.3 — Keep future modules empty of trading logic

- [ ] `dynamic_grid.rs` exists as a reserved module only.
- [ ] `volatility_grid.rs` exists as a reserved module only.
- [ ] `mean_reversion.rs` exists as a reserved module only.
- [ ] `breakout.rs` exists as a reserved module only.
- [ ] Do not guess signals, models, parameters, risk rules, or execution behavior.

### 3.7.4 — Preserve shared engine integration

- [ ] Update `src/trading/mod.rs` to expose the current static-grid types through the new strategy namespace.
- [ ] Existing Backtest/API/test imports continue to work without becoming strategy-location aware.
- [ ] No change to `Strategy`, Backtest engine, simulated execution, canonical persistence, or UI behavior.

### 3.7.5 — Verification

- [ ] Run Rust tests.
- [ ] Run the full frozen XAGUSDT regression unchanged.
- [ ] Confirm the semantic regression checksum remains unchanged.
- [ ] Deploy normally through Render.
- [ ] Update `Documents/ARCHITECTURE.md` and the master roadmap.

## Completion condition

Phase 3.7 is complete when strategy code has a durable one-strategy-per-module structure, the existing static-grid fixture behaves identically, the frozen regression remains unchanged, and Phase 4 can build Paper runtime against the shared `Strategy` interface without treating one grid file as the template for every future strategy.

After completion, proceed to **Phase 4 — Live Paper Runtime + Shared Trading Page**.
