# Phase 4.9 — BinanceGrid → BinanceBots project rename

## Objective

Rename the existing BinanceGrid project to **BinanceBots** as one controlled identity migration across the application branding, GitHub repository, Render service, Render public URL, and project documentation without changing trading behavior or losing any persisted market data, Bot configuration, Run history, authentication history, or production database state.

This is a rename of the existing production system, **not** a new deployment, new database, or new trading architecture.

## Target identity

| Surface | Current | Target |
| --- | --- | --- |
| Product / UI name | Binance Grid / BinanceGrid | BinanceBots |
| GitHub repository | `efealt/BinanceGrid` | `efealt/BinanceBots` |
| Render service | `BinanceGrid` | `BinanceBots` |
| Render URL | `https://binancegrid.onrender.com` | `https://binancebots.onrender.com` if Render makes that slug available |
| Browser/private-console branding | Binance Grid Private quant console | BinanceBots Private quant console |

## Rules

- Complete phases strictly in order.
- Rename the **existing** Render service. Do not create a replacement service merely to obtain a new name or URL.
- This project is still in development. Existing database contents, downloaded market data, Bot IDs, Run IDs, backtest history, Live-Paper history, authentication audit rows, and other test state are disposable unless the user explicitly says otherwise for a specific step.
- Prefer a clean implementation over backward compatibility. Do not add compatibility shims, migration bridges, aliases, legacy fallbacks, dual-read/dual-write paths, or preservation logic solely to keep development test state working.
- A fresh database is allowed and is the preferred rename path. All legacy **project-identity** forms must be renamed directly with no support for the old names: `BinanceGrid`, `Binance Grid`, `binance-grid`, `binance_grid`, `BINANCE_GRID_*`, old repository/service/URL identities, JavaScript namespaces, browser-storage keys, database filenames, package/binary names, thread/test names, and project-identifying user-agent strings.
- The word **grid** may remain only when it is semantically about a grid trading strategy, grid-specific research, grid parameters, or strategy module names such as `static_grid.rs`. Strategy terminology must not be renamed merely because the old project name contained “Grid”.
- Preserve only infrastructure that is useful to keep: the existing Render service/disk/region/plan/secrets can remain in place, but their stored test data does not need to survive the rename.
- Do not change strategy rules, execution semantics, risk behavior, Backtest behavior, Live-Paper behavior, or Live-Real-Account locking as part of this roadmap.
- Keep GitHub `main` as the source of truth and keep Render auto-deploy behavior unchanged except for the already-configured documentation Build Filters.
- Do not assume the desired Render subdomain is available. Verify it before treating the URL rename as complete; if Render cannot provide the target URL on the existing service, stop and report the exact limitation rather than creating a new service.
- Update `Documents/ARCHITECTURE.md` in the same implementation phase whenever the operational source-of-truth names or URLs change.

## Sequential phases

### Phase 1 — Rename inventory and safety baseline — COMPLETE

- [x] Inventory every user-visible and operational `BinanceGrid` / `Binance Grid` reference in HTML, JavaScript, Rust configuration, Cargo metadata, documentation, GitHub links, Render metadata, and repository instructions.
- [x] Record the current GitHub repository identity, Render service ID, Render persistent-disk ID/mount, production URL, branch, region, build command, start command, and production database path before changing anything.
- [x] Verify current development Bot/Run history and persistent database access only as a diagnostic baseline; those identifiers and records are not required to survive the rename.
- [x] Confirm the desired canonical spelling is exactly **BinanceBots** and the target repository is `efealt/BinanceBots`.

#### Phase 1 baseline

**Canonical target**

- Product name: **BinanceBots**
- Target GitHub repository: `efealt/BinanceBots`
- GitHub lookup of `efealt/BinanceBots` returned 404 before the rename, so there is no existing repository at that target name under the account.
- Target Render service name: **BinanceBots**
- Desired Render URL: `https://binancebots.onrender.com`, subject to availability on the existing Render service in Phase 4.

**Current GitHub source-of-truth baseline**

- Repository: `efealt/BinanceGrid`
- Branch: `main`
- Phase 1 baseline commit: `542c886978f0c0427ded4da23dbe0e3278f072fd`
- Current Rust package/binary identity: `binance-grid` → target `binance-bots`
- Current repository remains the same project; Phase 3 will rename it rather than creating a replacement repository.

**Current Render production baseline**

- Service name: `BinanceGrid`
- Service ID: `srv-daoni5f40ujc73bm4n1g`
- Service slug / URL: `binancegrid` / `https://binancegrid.onrender.com`
- Repository source: `https://github.com/efealt/BinanceGrid`
- Branch: `main`
- Region: Frankfurt
- Runtime: Rust
- Plan: `0.5c-512mb`
- Build command: `cargo build --release`
- Start command: `./target/release/binance-grid`
- Health check: `/api/health`
- Persistent disk ID: `dsk-daoo9ebm8hqs73fuqff0`
- Persistent disk mount: `/var/data`
- Persistent disk size: 1 GB
- Production database path: `/var/data/binance_grid.sqlite3` → target `/var/data/binance_bots.sqlite3`
- Documentation-only auto-deploy ignores already configured: `Roadmaps/**`, `Documents/**`, `README.md`

**Development persistence baseline**

Hosted Phase 4 acceptance proved that the persistent Render disk and database work correctly across reloads and deployments. Existing Bot/Run IDs and current stored market/backtest/auth data are development test state only. They are **not** continuity requirements for the rename.

The rename may delete and recreate the database, start Bot/Run numbering again, redownload market data, and regenerate test state whenever that produces a cleaner BinanceBots implementation. No database migration or backward-compatibility path is required unless the user explicitly requests preservation for a later production dataset.

**Rename inventory**

User-visible branding that should become **BinanceBots** in Phase 2:

- `web/index.html`
- `web/market.html`
- `web/data-downloader.html`
- `web/backtests.html`
- `web/trading.html`
- `web/security.html`

Each currently contains page-title, home-label, and/or header branding using `Binance Grid`.

Operational/current-project references to update in later phases:

- `AGENTS.md` — hosted application name.
- `README.md` — project heading.
- `Documents/APP.md` — application name/description.
- `Documents/ARCHITECTURE.md` — architecture title, application name, repository identity, and deployment references.
- `Roadmaps/04.8B-trading-workspace-production-revision.md` — completed historical/current product wording to classify during Phase 5.
- `Roadmaps/04.9-binancebots-project-rename.md` — intentionally contains both old and target identities for the migration plan.
- Render metadata — service name, repository source, slug/public URL.
- GitHub metadata — repository name and canonical repository URL.

Technical identifiers that **must** be directly renamed during this roadmap:

- Cargo package/binary: `binance-grid` → `binance-bots`.
- Production database: `/var/data/binance_grid.sqlite3` → `/var/data/binance_bots.sqlite3`.
- Local default database: `data/binance_grid.sqlite3` → `data/binance_bots.sqlite3`.
- Environment variables:
  - `BINANCE_GRID_DATABASE_PATH` → `BINANCE_BOTS_DATABASE_PATH`
  - `BINANCE_GRID_AUTH_MODE` → `BINANCE_BOTS_AUTH_MODE`
  - `BINANCE_GRID_AUTH_USERNAME` → `BINANCE_BOTS_AUTH_USERNAME`
  - `BINANCE_GRID_AUTH_PASSWORD` → `BINANCE_BOTS_AUTH_PASSWORD`
- Internal JavaScript namespace: `BinanceGridTradingContract` → `BinanceBotsTradingContract`.
- Browser local-storage keys: `binance-grid-...` → `binance-bots-...`.
- Rust thread/test/temp-file names using `binance-grid-...` → BinanceBots equivalents.
- Project-identifying HTTP user-agent strings such as `binance-grid-research-archive-importer/1.0` → BinanceBots equivalents.
- Active documentation, comments, fixture descriptions, CI text, and operational messages that use BinanceGrid as the **project identity** → BinanceBots.

No alias, fallback, dual-read, dual-write, environment-variable compatibility, old database-file lookup, or old JavaScript namespace support is permitted.

Historical Git commit messages do not need rewriting. The word **grid** remains valid when it describes actual grid strategy logic, strategy modules, parameters, research, or tests.

**Exit achieved:** We have a complete rename map, a verified pre-rename GitHub/Render/persistence baseline, confirmed target identity **BinanceBots**, and no production naming changes have been made yet.

### Phase 2 — Application and technical identity rename — COMPLETE

**Clean-start rule: no backward-compatible aliases, dual branding, legacy environment variables, old database lookup, or old JavaScript namespace support.**

- [x] Replace user-visible Binance Grid / BinanceGrid branding with **BinanceBots** across the authenticated application, login/logout surfaces, page titles, navigation/header branding, Console, Trading, Market, Data Downloader, Backtest, Security, and relevant user-facing messages.
- [x] Rename the Rust package/binary from `binance-grid` to `binance-bots`.
- [x] Rename the database defaults to `/var/data/binance_bots.sqlite3` in production and `data/binance_bots.sqlite3` locally. Start from a fresh database; do not migrate or read the old `binance_grid.sqlite3`.
- [x] Rename all active `BINANCE_GRID_*` environment-variable contracts to `BINANCE_BOTS_*` with no legacy fallback.
- [x] Rename project-identity JavaScript namespaces, browser-storage keys, Rust thread/test/temp-file names, project-identifying user agents, comments, fixture descriptions, CI text, and operational messages to BinanceBots equivalents.
- [x] Update browser titles, accessible labels, and private-console wording consistently.
- [x] Keep trading terminology unchanged: Backtest, Live-Paper, and Live-Real-Account retain their current exact meanings. Keep grid terminology wherever it genuinely refers to grid strategy logic.
- [x] Run a repository-wide search for `BinanceGrid`, `Binance Grid`, `binance-grid`, `binance_grid`, and `BINANCE_GRID_` and eliminate every active project-identity occurrence before Phase 2 closes.
- [x] Verify all major pages at wide and narrow desktop widths and confirm no strategy rules, API route semantics, Backtest behavior, Live-Paper behavior, or Live-Real-Account locking changed.

**Exit:** Application code and technical identity consistently use BinanceBots naming, the fresh database is `binance_bots.sqlite3`, and no active legacy BinanceGrid project-identity names remain.

### Phase 3 — GitHub repository rename — COMPLETE

**Clean-start rule: do not maintain duplicate repositories, compatibility remotes, or parallel old/new repository identities.**

- [x] Rename the existing GitHub repository from `efealt/BinanceGrid` to `efealt/BinanceBots`; do not create a second repository.
- [x] Verify `main`, commit history, Actions history/configuration, repository files, branch settings, and access remain intact after the rename.
- [x] Verify the Render service still resolves/pulls the renamed GitHub repository correctly before making any Render naming change.
- [x] Update repository-internal hard-coded GitHub URLs that should point to the new canonical repository name.

**Exit achieved:** GitHub source of truth is `efealt/BinanceBots`, with the same repository ID/history and a working Render source connection. The old GitHub repository URL redirects to the renamed repository.

### Phase 4 — Render service and public URL rename — DEFERRED BY USER

**Decision:** Keep the existing Render service identity and URL for now. The product/code/GitHub identity is BinanceBots, but Render remains `BinanceGrid` at `https://binancegrid.onrender.com`. Do not create a replacement Render service solely to obtain a matching hostname. Revisit only if the user explicitly asks in the future.**

- [x] Rename the **existing** Render service from BinanceGrid to **BinanceBots** while keeping the same service ID/region/plan/disk where convenient.
- [x] Change the Render start command to `./target/release/binance-bots`.
- [x] Replace Render `BINANCE_GRID_*` environment variables with the corresponding `BINANCE_BOTS_*` names and remove the old variables.
- [x] Point production at the fresh `/var/data/binance_bots.sqlite3` database; do not migrate or reuse `/var/data/binance_grid.sqlite3`.
- [x] Verify whether the existing service receives or can be assigned the target public URL `https://binancebots.onrender.com`.
- [x] If the target Render URL is available, make it canonical and verify authentication, static assets, HTTP APIs, WebSockets, and `/api/health` through the new URL.
- [x] Verify the persistent disk is usable at `/var/data`. Existing database contents do not need to survive; a fresh BinanceBots database is acceptable and may be preferred.
- [x] Do not create a replacement Render service to force the URL. If the target subdomain is unavailable on the existing service, retain the working service and document the exact URL outcome for a separate naming decision.

**Phase 4 decision note:** The existing Render service keeps the legacy infrastructure identity `BinanceGrid` and public URL `https://binancegrid.onrender.com`. This is intentional. Render's generated hostname is not being migrated now because that would require a replacement service. Do not attempt to change it unless the user explicitly reopens this decision.

**Exit:** BinanceBots application/code/GitHub identity is complete while the existing Render service/URL intentionally remain `BinanceGrid` / `https://binancegrid.onrender.com`. No replacement service is created.

### Phase 5 — Operational references and architecture source of truth

**Clean-start rule: current operational docs should describe only the new BinanceBots system; do not document compatibility procedures for obsolete development names.**

- [ ] Update `AGENTS.md`, `Documents/ARCHITECTURE.md`, README, relevant Roadmaps, repository links, Render links, and operational instructions to use the final BinanceBots names and canonical URLs.
- [ ] Preserve historical roadmap prose where the old name is necessary to describe an already-completed historical state; otherwise use BinanceBots as the current project name.
- [ ] Record explicitly that the active database is `binance_bots.sqlite3`, active environment variables are `BINANCE_BOTS_*`, and the active binary/package is `binance-bots`.
- [ ] Search the full repository for `BinanceGrid`, `Binance Grid`, `binance-grid`, `binance_grid`, and `BINANCE_GRID_`. Any remaining active project-identity occurrence is an error to fix; only genuine grid-strategy terminology or immutable Git history may remain.

**Exit:** Current operational documentation, architecture, links, and instructions all point to the BinanceBots identity with no ambiguous active naming.

### Phase 6 — Production acceptance and rename closeout

**Clean-start rule: acceptance proves the clean BinanceBots system works; it does not require old development Bot/Run IDs, test data, database contents, or legacy technical names to survive.**

- [ ] Verify login/logout and private-page protection through the final canonical URL.
- [ ] Verify Console, Market, Data Downloader, Backtest, Trading, Security, `/api/health`, authenticated APIs, and WebSocket streams work after the rename.
- [ ] Verify BinanceBots can create, persist, reload, inspect, stop, and restart fresh Bots/Runs correctly after the rename. Old development IDs/history need not exist.
- [ ] Start and stop one new Live-Paper Run under BinanceBots and verify it uses the same canonical persistence/runtime model as before the rename.
- [ ] Verify GitHub Actions are green and Render is LIVE with no new runtime errors.
- [ ] Compare the post-rename system with Phase 1 for architecture/execution semantics only. Database resets and regenerated development data are acceptable; strategy/execution behavior must remain correct.
- [ ] Mark Phase 4.9 complete and return to master Phase 5 — Paper-period historical replay.

**Exit:** BinanceBots is the canonical application, GitHub, Render, and technical identity; the clean development system works end to end with no unnecessary backward-compatibility baggage; and the system is ready to continue with Phase 5.
