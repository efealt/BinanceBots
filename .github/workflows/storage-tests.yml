name: Core trading tests

on:
  push:
    branches: [main]
    paths:
      - "src/storage/**"
      - "src/backtest.rs"
      - "src/backtest/**"
      - "src/paper.rs"
      - "src/paper/**"
      - "src/market/**"
      - "src/main.rs"
      - "src/trading/**"
      - "src/api/**"
      - "web/backtests.html"
      - "web/backtests.js"
      - "web/trading.html"
      - "web/trading.js"
      - "web/trading-contract.js"
      - "scripts/test-trading-shell.js"
      - "scripts/test-trading-contract.js"
      - "web/backtest-analysis-math.js"
      - "scripts/test-backtest-analysis.js"
      - "web/styles.css"
      - "migrations/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "test-data/**"
      - ".github/workflows/storage-tests.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Check backtest JavaScript syntax
        run: |
          node --check web/backtests.js
          node --check web/backtest-analysis-math.js
          node --check web/trading.js
          node --check web/trading-contract.js
      - name: Test backtest analysis math
        run: node scripts/test-backtest-analysis.js
      - name: Test Trading page shell
        run: node scripts/test-trading-shell.js
      - name: Test Trading shared mode contract
        run: node scripts/test-trading-contract.js
      - name: Run Phase 4 parity + safety verification
        run: cargo test phase4_ -- --nocapture
      - name: Run Phase 5 execution isolation verification
        run: cargo test phase5_ -- --nocapture
      - name: Run Rust tests
        run: cargo test --all-targets
      - name: Run full frozen XAGUSDT grid regression
        run: |
          fixture="test-data/xagusdt_1m_2026.sqlite3"
          before="$(sha256sum "$fixture" | awk '{print $1}')"
          test "$before" = "37afda792f5f06cee9e63eca010f6c5b850d2282fe5ecc5a686b213f832c4a7e"
          regression_db="$(mktemp --suffix=.sqlite3)"
          cp "$fixture" "$regression_db"
          XAGUSDT_REGRESSION_DB="$regression_db" cargo test full_xagusdt_grid_regression -- --ignored --nocapture
          after="$(sha256sum "$fixture" | awk '{print $1}')"
          test "$after" = "$before"
          rm -f "$regression_db" "$regression_db-wal" "$regression_db-shm"
